#!/usr/bin/python3
def square_matrix_simple(matrix=[]):
    if matrix is not None:
        squares = []
        for i in matrix:
            new.append(list(map(lambda x: x**2, i)))
        return (squares)
    return None
